# Production Environment Layer
# Warn logging, optimized performance, minimal overhead

[metadata]
name = "prod"
category = "env"
description = "Production environment"
use_cases = [
    "Production deployments",
    "High availability",
    "Performance optimization",
]
features = [
    "TODO: Add features"
]


[profile]
name = "Production Environment"
description = "Optimized configuration for production deployment"

[debug]
enabled = false
level = "warn"

[debug.output]
stdout = false
colored = false
format = "json"

[runtime]
script_timeout_seconds = 300
max_concurrent_scripts = 100

[storage]
# Production deployments should override these via env vars or explicitly here
# base_dir = "/app/data"
